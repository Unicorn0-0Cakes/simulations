"""Population-cap exercises for the civilization simulation."""

MAX_POPULATION = 100_000


def admit_births(current_population: int, requested_births: int) -> int:
    """Return the number of births that may be admitted.

    Requirements:
    - ``current_population`` must be between 0 and ``MAX_POPULATION``, inclusive.
    - ``requested_births`` must be non-negative.
    - Raise ``ValueError`` when either input violates those constraints.
    - Return all requested births when capacity permits.
    - Otherwise return only the number that fits.
    - The return value must always be a non-negative integer.

    This function is intentionally unfinished. Implement it without asking an AI
    to generate the solution. Explanations, debugging questions, and hints are
    allowed during Guided Mode.
    """
    raise NotImplementedError("Exercise 1: implement admit_births")
